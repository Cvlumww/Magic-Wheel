import React, { useState, useContext } from "react";

import { AppContext, AppProvider, usePaulContext } from "../../AppContext";

const plinko = () => {
  // const { spinAnimation, message, result, setResult, showPop } =
  // usePaulContext();

  return (
    <div className="plinkoMain">
      <h1>Old Plinko Idea: now move it inside message!</h1>
    </div>
  );
};

export default plinko;
